org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# Tenant identification (Model B — white-label build). One tenant per APK.
# Change these + app name/colors per client; everything else comes from the API.
TENANT_BASE_URL=https://nb-api.example.com/
